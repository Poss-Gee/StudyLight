rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for logged-in users.
    // WARNING: This is for development only and is not secure.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
